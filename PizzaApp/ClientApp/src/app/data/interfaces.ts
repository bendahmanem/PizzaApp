export interface Pizza {
    NomPizza: string;
    TaillePizza: number;
    PrixPizza: number;
}

export interface PizzaResponse {
    numPizza: number;
    nomPizza: string;
    taillePizza: number;
    prixPizza: number;
}